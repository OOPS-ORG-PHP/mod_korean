<FONT ID=title>human_fsize_lib()</FONT>

(required korean module)

human_fsize_lib -- byte 값을 읽기 쉽게 변환

<FONT ID=RE>설명</FONT>

<B>string human_fsize_lib (int byte [, int sub ])</B>

byte 의 값을 이함수를 거쳐 byte, kbyte, mbyte 로 자동으로 표현을 해 준다. sub 의 값은 1 과 0
을 가지며 1 일 경우에는 변환된 값 뒤에 원 byte 값을 표시를 해 준다.

<FONT ID=RE>예제</FONT>

$a = human_fsize_lib(1024);
echo $a; =&gt; 1 KB

$a = human_fsize_lib(1024,1);
echo $a; =&gt; 1 KB (1024 Bytes) 
