<span class="title">autolink_lib()</span>

(required korean module)

autolink_lib -- TEXT 중 URL 이나 EMAIL 을 자동으로 링크

<span id="RE">설명</span>

<b>string autolink_lib(string str)</b>

문자열에 들어 있는 url 과 email 주로를 자동으로 hyper link 를 만든다.

<span id="RE">예제</span>

$str = autolink_lib($str);
